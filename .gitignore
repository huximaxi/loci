node_modules/
dist/
.DS_Store
*.local
.env
.env.*
!.env.example
server/waitlist.db
server/venv/

# Tauri
src-tauri/target/
*.dmg
*.exe
*.app

# Vite build cache
*.timestamp-*.mjs
.vite/

# IDE
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln

# ── LOCAL ONLY — never push to public repo ─────────────────────
# JUMP-IN prompts + local planning docs (Loci Dev on Hux local)
pipeline/features/JUMP-IN-*.md
pipeline/features/LONGLIST.md
pipeline/features/session-start-gate.md
pipeline/features/node-entry-ritual.md
pipeline/features/1A-ollama/NYX-DESIGN-PROMPTS.md

# Local pipeline state + logs
pipeline/rune-funnel-l1/
pipeline/watcher/
pipeline/ROADMAP-TRACKER.md.bak
watcher/

# Tauri generated files
desktop/src-tauri/gen/

# Claude Cowork local session
.claude/

# Palace memory — local only, must never leave the dev machine
_palace/

# Session notes — local only
.session-pov-*.md

# Stale workspace backup files
Cargo.lock.pre-workspace
desktop/src-tauri/Cargo.lock.pre-workspace

# Internal design exploration notes (studio ideation)
desktop/DESIGN-IDEAS-*.md

# Pre-strip/backup files from landing migrations
landing/*.pre-strip-backup
landing/_audit.md
