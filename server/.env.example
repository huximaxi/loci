# Copy to .env and fill in before deploying
LOCI_ADMIN_TOKEN=change-me-strong-random-token

# SMTP — optional, for Hux signup notifications
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
NOTIFY_EMAIL=
